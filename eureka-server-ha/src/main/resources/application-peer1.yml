spring:
  application:
    name: eureka-server-ha

server:
  port: 8760

ha:
  port: 8761

eureka:
  server:
#    enable-self-preservation: false
  instance:
    hostname: peer1
  client:
#    registerWithEureka: false
#    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${ha.port}/eureka/